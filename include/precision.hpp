#pragma once

#include <H5Cpp.h>

// Need to also change the typedef in FAFS_PROGRAM in src/kernels.cpp!
// Need to also change H5 array type in Array constructor!
typedef float real;

//typedef double real;
